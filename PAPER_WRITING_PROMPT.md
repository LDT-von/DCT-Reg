# DCT-Reg 论文写作提示词

**生成时间**: 2026-09-08
**状态**: 准备执行

---

## 📌 写作核心原则

**原则1**: 性能优先，可解释性次之
**原则2**: OT是技术手段，不是独立贡献
**原则3**: 诚实承认可解释性的局限性

---

## 📝 Abstract 提示词

```
请根据以下结构撰写 Abstract：

1. 背景 (2句)
   - 多模态生存预测的挑战：病理切片和分子通路的融合
   - 现有方法的局限

2. 方法 (3句)
   - DCT 是一个多模态生存预测框架
   - 核心技术：语义对齐的多模态融合 + OT 运输
   - 训练目标：NLL + IPCW 排序损失

3. 结果 (2句)
   - 5个TCGA癌种：平均 C-index 0.703，优于SlotSPE的0.697
   - IPCW排序贡献最大（+0.04）

4. 结论 (1句)
   - DCT在预测性能上取得SOTA，同时提供可审计的决策机制
```

---

## 📝 Introduction 提示词

### Paragraph 1: 背景和问题 (4-5句)

**提示词**:
```
多模态学习在肿瘤预后预测中具有重要价值，因为病理图像和分子通路提供了互补的生物学信息。

然而，现有的多模态生存预测方法面临两个核心挑战：
(1) 如何有效融合异构数据（高分辨率病理切片 vs 高维分子特征）
(2) 如何确保模型决策的可解释性

本文提出 DCT，一个基于最优传输的多模态生存预测框架。
```

### Paragraph 2: 方法概述 (4-5句)

**提示词**:
```
DCT 的核心创新在于使用共享语义原型字典对齐病理切片patch和分子通路。

通过 Sinkhorn 最优传输，模型学习将患者特异的特征运输到语义坐标空间。

训练目标结合了负对数似然损失和 IPCW 加权排序损失。

实验表明，这种方法在5个TCGA癌种上取得了平均 C-index 0.703。
```

### Paragraph 3: 可解释性 (2-3句)

**提示词**:
```
除了预测性能，DCT 还提供了模型决策的审计机制。

通过在代价空间中构造干预，我们可以分析模型对不同风险方向的响应。

注意：这种审计不等同于因果效应声明，只是模型行为的观察性分析。
```

### Paragraph 4: 贡献总结 (3-4句)

**提示词**:
```
本文的主要贡献包括：
(1) 提出 DCT 框架，在5个TCGA癌种上优于或持平现有方法
(2) 验证 IPCW 排序损失对预测性能的贡献
(3) 提供可审计的决策机制，分析模型内部响应

代码和预训练模型已公开。
```

---

## 📝 Method 提示词

### Section 1: 模型架构

**提示词**:
```
DCT 的架构包含三个主要组件：
(1) WSI encoder：将病理切片patch编码为特征向量
(2) Omics encoder：将分子通路编码为特征向量  
(3) OT fusion module：通过最优传输融合多模态特征

关键设计：共享语义原型字典 K=8，作为跨模态对齐的桥梁。
```

### Section 2: 训练目标

**提示词**:
```
DCT 的训练目标为：

L = L_NLL + λ_rank * L_IPCW-rank + λ_dir * L_direction

其中：
- L_NLL: 离散时间生存负对数似然
- L_IPCW-rank: 逆概率审查加权的成对排序损失（Uno风格）
- L_direction: 方向一致性损失（可选，默认权重0.05）

消融实验表明，IPCW 排序损失贡献最大（+0.04 C-index）。
```

### Section 3: 决策审计机制

**提示词**:
```
DCT 提供决策审计机制，通过在代价空间中构造干预来分析模型行为。

给定一个患者，审计过程包括：
(1) 沿低风险方向构造干预序列
(2) 沿高风险方向构造干预序列
(3) 观察模型预测的变化

这种审计可以揭示模型决策是否与预期的风险方向一致。

**重要声明**：这种审计是观察性分析，不构成因果效应声明。
模型响应的存在与否不能直接推断因果关系。
```

---

## 📝 Results 提示词

### Table 1: 主要结果

**提示词**:
```
请创建以下格式的表格：

| 癌种 | DCT C-index | SlotSPE | 差值 |
|------|------------|---------|------|
| BLCA | 0.721 ± 0.016 | 0.708 | +0.013 |
| HNSC | 0.647 ± 0.071 | 0.642 | +0.005 |
| KIRC | 0.858 ± 0.012 | 0.815 | +0.043 |
| LUSC | 0.631 ± 0.055 | 0.634 | -0.003 |
| SKCM | 0.656 ± 0.047 | 0.688 | -0.032 |
| 平均 | 0.703 | 0.697 | +0.006 |

平均 C-index 0.703 优于 SlotSPE 的 0.697（差值 +0.006）。
```

### Table 2: 消融实验

**提示词**:
```
请创建以下格式的消融实验表格：

| 配置 | BLCA C-index | 说明 |
|------|-------------|------|
| Full (NLL + IPCW + Direction) | 0.7208 | 完整模型 |
| NLL-only | 0.6286 | 仅负对数似然 |
| IPCW-only | 0.5914 | 仅IPCW排序 |
| Direction-only | 0.5697 | 仅方向损失 |

**关键发现**: IPCW 排序损失贡献最大（Full - NLL-only = +0.09）。
```

### Figure 1: 决策审计可视化

**提示词**:
```
请绘制决策审计的可视化示例：

图(a): 沿低风险方向的剂量响应曲线
图(b): 沿高风险方向的剂量响应曲线

说明：
- X轴：干预强度 α (0到1)
- Y轴：预测风险分数
- 阴影区域：95%置信区间

**图注**：沿风险方向的预测变化显示模型对不同干预的响应。

**重要**：这种响应是观察性的，不能直接推断因果效应。
```

---

## 📝 Discussion 提示词

### Paragraph 1: 主要发现总结 (3-4句)

**提示词**:
```
DCT 在5个TCGA癌种上取得了平均 C-index 0.703，优于 SlotSPE 的 0.697。

消融实验表明 IPCW 排序损失是主要贡献项。

决策审计提供了模型行为的观察性分析，但不能声称因果效应。
```

### Paragraph 2: 局限性 (3-4句)

**提示词**:
```
本文存在以下局限性：

(1) 可解释性声明受限：当前审计显示方向一致性率约52.6%，接近随机水平。
    这表明传输机制对预测的直接贡献有限。

(2) 因果效应不成立：本文的决策审计是观察性分析，不构成因果效应声明。
    我们不能声称"改变模型输入会导致风险变化"。

(3) 多癌种验证有限：仅在5个TCGA癌种上验证，泛化性待进一步研究。

这些局限性需要在投稿时如实披露。
```

### Paragraph 3: 未来工作 (2-3句)

**提示词**:
```
未来工作可以从以下方向改进：

(1) 增强传输机制：当前的方向传输权重(0.05)较小，可尝试更大的权重。

(2) 锚点优化：使用生物学先验或对比学习优化锚点质量。

(3) 因果推断框架：引入更强的因果推断方法，使可解释性声明更加有力。
```

---

## 📝 写作注意事项

### ❌ 避免的表述

1. **不要声称因果效应**:
   - ❌ "DCT 可以推断改变哪些因素会降低风险"
   - ❌ "模型学习到了因果关系"
   - ✅ "模型对干预的响应显示..."

2. **不要夸大可解释性**:
   - ❌ "DCT 提供可解释的预测"
   - ✅ "DCT 提供模型决策的审计机制"

3. **不要过度强调 OT**:
   - ❌ "OT 是 DCT 的核心贡献"
   - ✅ "OT 是实现多模态融合的技术手段"

### ✅ 推荐使用的表述

1. **预测性能**:
   - "DCT 在 X 癌种上达到 C-index Y，优于现有方法"

2. **可解释性**:
   - "通过决策审计，我们观察到..."
   - "模型对风险方向的响应显示..."
   - "这种审计揭示了模型内部决策机制的一致性/不一致性"

3. **OT 定位**:
   - "OT 用于实现语义对齐"
   - "OT 是技术实现，不是独立贡献"

---

## 📋 论文结构模板

```
1. Abstract
2. Introduction
   2.1 Background
   2.2 Method Overview
   2.3 Interpretability
   2.4 Contributions
3. Method
   3.1 Model Architecture
   3.2 Training Objectives
   3.3 Decision Auditing Mechanism
4. Experiments
   4.1 Datasets
   4.2 Main Results
   4.3 Ablation Study
   4.4 Decision Auditing
5. Discussion
   5.1 Summary
   5.2 Limitations
   5.3 Future Work
6. Conclusion
```

---

## 📞 如有疑问

- **关于实验数据**: 查看 `best_epochs_blca.json` 和 `ablation_summary_blca_5fold.json`
- **关于审计结果**: 查看 `results/audit_best_epochs_blca/audit_summary.json`
- **关于贡献定位**: 遵循"性能优先，可解释性次之"原则

---

**写作愉快！** ✍️
