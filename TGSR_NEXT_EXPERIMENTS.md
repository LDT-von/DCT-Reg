# v3.2 TGSR 后续实验计划

**更新时间**: 2026-09-06 05:50 UTC
**状态**: 待执行

---

## ⚠️ 重要前提

**本实验计划严格区分两个独立问题：**

| 问题 | 验证方法 |
|------|---------|
| **预测有效性**：TGSR是否能改善生存预测？ | C-index, IBS, iAUC |
| **方向解释正确性**：低/高风险锚点是否可靠引导风险方向？ | DCR, DMR, Plan TV |

> C-index提升**不能**证明方向假设正确。两者需要分别验证。

---

## 实验顺序（按优先级）

### 🔴 优先级 1：查清指标异常

**问题**：OT反馈的C-index提高了，但IBS变差、iAUC降低（尤其Fold 1, 2）

**需要检查**：
1. 生存曲线质量（预测分布是否合理）
2. 评估实现（IBS/iAUC计算代码）
3. 时间范围设置（time_bins是否一致）
4. 预测分布变化对指标敏感度的影响

**输出**：异常原因分析报告

---

### 🟡 优先级 2：TGSR + 排序损失

**实验代码**：`scripts/run_dct_v32_ranking_ablation.py`（待创建）

**实验设计**：

| 实验 | 损失函数 | 预期 |
|------|---------|------|
| A | TGSR + NLL only | 当前基线 (0.7057) |
| B | TGSR + NLL + IPCW排序 | 待测 |
| C | TGSR + NLL + λ_rank sweeps | 待测 |

**关键参数**：
- `dct_lambda_ipcw_rank`: [0.0, 0.1, 0.5, 1.0]
- 保持其他参数不变
- 使用相同fold划分和训练预算

**验证指标**：
- C-index（主要）
- IBS, iAUC（次要）
- 确认指标异常是否因排序损失而改善

---

### 🟡 优先级 3：单独测试方向损失

**实验设计**：

| 实验 | 损失函数 | 预期 |
|------|---------|------|
| D | TGSR + NLL + 方向损失 | 待测 |
| E | TGSR + NLL + IPCW + 方向损失 | 待测 |

**关键参数**：
- `dct_v38_lambda_direction`: [0.0, 0.01, 0.05, 0.1]

**验证**：
1. 预测指标：C-index是否提升？
2. 方向审计：DCR, DMR是否通过？
3. **两者必须同时满足**才能声称方向损失有效

**原则**：不能因为方向损失属于v3.10就默认有效。

---

### 🟢 优先级 4：可学习反馈强度

**如果现有实现已包含门控**：
1. 检查门控实际学到了什么（权重分布、可视化）
2. 避免重复添加模块

**如果需要添加**：
```python
# 伪代码：可学习的反馈强度
gamma = sigmoid(gate_network(slot_state))  # 门控网络
reaggregated = gamma * ot_context + (1 - gamma) * self_context
```

**对比实验**：
- 固定γ=0.25 vs 学习γ

---

## 实验配置规范

**所有实验必须遵循**：

| 规范 | 要求 |
|------|------|
| Fold划分 | 与本轮相同（5fold_uni2h） |
| 训练预算 | 30 epochs，同seed=3 |
| 调参方式 | 验证集选择最佳epoch |
| 评估方式 | 未参与选择的验证数据 |
| 记录 | 完整epoch曲线、指标日志 |

---

## 论文贡献定位

根据实验结果，选择以下路线之一：

### 路线A：方向审计通过
```
贡献："具有方向响应约束的运输模型"
- 强因果/解释性主张
- 需要明确适用条件
```

### 路线B：方向审计失败，但TGSR预测增益稳定
```
贡献："运输引导的槽重聚合改善生存预测"
- 聚焦预测性能
- 收缩锚点方向解释主张
```

---

## 执行清单

- [ ] **Step 1**: 查清IBS/iAUC异常原因
- [ ] **Step 2**: 运行TGSR + 排序损失实验
- [ ] **Step 3**: 运行TGSR + 方向损失实验
- [ ] **Step 4**: 方向审计（如Step 3预测提升）
- [ ] **Step 5**: 根据结果确定论文路线

---

## 资源预估

| 实验 | 任务数 | 预计时长 |
|------|--------|---------|
| 排序损失消融（4个λ值） | 4×5=20 | ~14小时 |
| 方向损失消融（4个λ值） | 4×5=20 | ~14小时 |
| 组合实验（最佳λ组合） | 2×5=10 | ~7小时 |
| 方向审计 | 3×5=15 | ~3小时 |
| **总计** | ~65任务 | ~38小时 |
