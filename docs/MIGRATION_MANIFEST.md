# 迁移清单

迁移源：`E:\SurvOT-Rank`，目标：独立仓库 `DCT-Reg`。

- 迁移时源库 HEAD：`ec9d13bfaa4fe476be04e7fc61ab1a51272d4cff`。
- DCT v3.10 首次冻结实现提交：`a2a5f2cbe6a1e86516394e19a229af9a7ae87f91`。
- 源库当时含未跟踪研究材料；本清单只声明本仓库实际纳入的文件，不把整个 working tree 描述为可复现快照。

## 后续同步

- 2026-08-26：检查并快进 SurvOT-Rank 到 `271466d9d3375a061270218f0b1502ffa8f58723`。
- 该提交只新增 direction-loss 两折验证汇总。其数字被迁入 `experiments/candidate_evidence/` 并标为隔离证据；未把“显著”结论或正式完成状态迁入。

## 已迁移

- DCT v3.10 最终类及其最小可执行继承链。
- 共享训练器、配置解析、数据加载和 SlotSPE 兼容运行时。
- 冻结 v3.10 配置、最终 6 癌种 × 5 fold 队列、2×2 消融与机制控制队列。
- 持出集机制审计脚本。
- 唯一 Markdown/Word 论文草稿。
- 证据门、复现协议和实验登记表。

## 刻意未迁移

- 其他候选方法、废弃分支、临时脚本、缓存、训练结果目录和重复论文版本。
- v3.8.2 结果文件及其“Final”命名；它们不是 v3.10 证据。
- WSI 特征、临床数据和 splits。数据需单独授权和版本管理。
- 源库中不存在的 `5fold_uni2h`；绝不以 `5fold` 重命名替代。

## 迁移时修复

独立库修复了 fixed-coupling 控制的 batch 缓存回归：事实计划始终由当前 batch 新求解，缓存只供当前 batch 的干预分支重放。对应回归测试覆盖不同 batch size 和事实计划等价性。
